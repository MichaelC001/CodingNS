# 20260915 验收记录

## 已完成验证

- `pnpm install --frozen-lockfile`：安装完成，未触发 better-sqlite3/node-pty 编译脚本。
- `pnpm check:sqlite-runtime`：通过，没有直接使用 `node:sqlite`。
- `pnpm --dir packages/session-sync-core build`：通过。
- `pnpm --dir apps/host build`：通过。
- `pnpm --dir apps/host test tests/modules/affairs-indexer/sqlite-runtime.test.ts`：2/2 通过。
- `pnpm --dir apps/host test tests/integration/sqlite-bootstrap.test.ts`：21/21 通过。
- `pnpm --dir apps/host test tests/integration/terminal-service-create.test.ts tests/integration/terminal-output-buffer.test.ts`：3/3 通过。
- `node --test packages/codingns/tests/build-script.test.mjs`：9/9 通过。
- `bash -n install.sh`、官网两份安装脚本和 Windows 回放脚本：通过。
- `pnpm --dir packages/codingns build`：通过。
- Node 22 本机 SQLite/PTY smoke：通过。
- 删除旧 `@codingns/node-pty` fork、Node 22 SQLite vendor、Node 22 runtime 脚本和历史测试；Host 适配器已改名为 `sqlite-runtime.ts`，根目录与官网两份安装脚本统一使用当前 Node、`@lydell/node-pty` 和 `libsql`。

## 兼容性说明

`libsql@0.5.29` 和 `@lydell/node-pty@1.1.0` 使用预编译 N-API 平台包，不需要安装期本机编译。Node 24/26 的候选包加载和 PTY/SQLite 基础读写已在前置验证中通过；当前验收机只提供 Node 22，未重复执行 Node 24/26 安装回放。

## 已知边界

- `@lydell/node-pty` 不再提供 CodingNS fork 中旧 Windows winpty fallback；旧于供应商支持矩阵的 Windows 系统不在本次范围内。
- 官网安装脚本仍保留“原生模块预编译下载失败时需要本机工具”的诊断提示，但不会再切换 Node 版本或回退到未加 scope 的 `node-pty`。
